export const signout = (next) => {
   if (typeof window !== 'undefined') localStorage.removeItem('jwt')
   next()
   return fetch('localhost:8080/signout', {
       method: 'GET'
    })
        .then(response => {
            return response.json()
        }
        .catch(err => console.log(err))
}